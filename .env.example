# Copy to .env and fill only the keys you actually use.
# NEVER commit .env or real API keys.

# LLM fallback chain
GEMINI_API_KEY=
GROQ_API_KEY=
DEEPSEEK_API_KEY=

# Optional: raises GitHub API rate limits for paper -> repository enrichment
GITHUB_TOKEN=
